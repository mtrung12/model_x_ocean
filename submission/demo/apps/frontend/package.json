{
  "name": "ocean-demo-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Static vanilla-JS frontend for the OCEAN pipeline demo. No bundler — served directly by the FastAPI backend.",
  "scripts": {
    "lint": "echo 'No linter configured (vanilla JS, no build step)'",
    "dev": "echo 'Frontend is served by the backend — run: make dev'"
  }
}
